# workspace Development Guidelines

## Important

- Always save `.claude/` files (skills, settings, etc.) in the repository (`/workspace/.claude/`), never in `~/.claude/`.

## Branching Strategy

This project follows GitFlow. See `/branch-strategy` skill for full details.

## Active Technologies

- Go
- SOPS
- age
- AWS CLI
