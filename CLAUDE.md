# workspace Development Guidelines

## Important

- Always save `.claude/` files (skills, settings, etc.) in the repository (`/workspace/.claude/`), never in `~/.claude/`.

## Active Technologies
- Go (latest stable) + aws-sdk-go-v2, standard library (037-remove-store-flag)
- AWS SSM Parameter Store (via SSMStore) (037-remove-store-flag)

- Go
- SOPS
- age
- AWS CLI
