# workspace Development Guidelines

## Important

- Always save `.claude/` files (skills, settings, etc.) in the repository (`/workspace/.claude/`), never in `~/.claude/`.

## Active Technologies
- Bash (POSIX-compatible shell script) + psm, aws CLI, sops, age (all pre-installed in DevContainer) (021-verify-quickstart-scenarios)
- AWS SSM Parameter Store (sandbox account) (021-verify-quickstart-scenarios)

- Go
- SOPS
- age
- AWS CLI
